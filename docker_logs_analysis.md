# Анализ логов Docker Compose - Обнаруженные ошибки

**Дата анализа:** 2026-01-20 20:28
**Дата исправления:** 2026-01-20 20:36
**Статус сервисов:** 7 контейнеров запущены, 1 unhealthy

---

## ✅ ИСПРАВЛЕНО

### Ошибка 400 при переключении агентов - ИСПРАВЛЕНА

**Файл:** `codelab-ai-service/agent-runtime/app/domain/services/message_orchestration.py`

**Проблема:**
При переключении агентов через tool `switch_mode`, система сохраняла assistant message с tool_call в историю, но НЕ добавляла соответствующий tool_result. При следующем запросе к LLM провайдер (OpenRouter/Azure) не находил результат выполнения tool_call и возвращал ошибку 400.

**Решение:**
Добавлен код для автоматического создания tool_result после переключения агента. Теперь при обнаружении `switch_agent` chunk система:
1. Находит последний tool_call для `switch_mode` в истории
2. Добавляет tool_result с сообщением "Switched to {agent} agent"
3. Переключает агента
4. Продолжает обработку с новым агентом

**Изменения:**
- Добавлена логика поиска call_id для switch_mode tool_call
- Добавлен вызов `add_tool_result()` перед переключением агента
- Исправлено в двух местах: при обычном переключении и при переключении после tool_result

**Статус:** ✅ Исправлено и развернуто (agent-runtime пересобран)

---

## 🔴 Критические ошибки

### 1. **Nginx - Unhealthy Status**
**Сервис:** `codelab-ai-service-nginx-1`  
**Статус:** `Up About a minute (unhealthy)`  
**Проблема:** Nginx не проходит health check, хотя контейнер запущен

**Рекомендации:**
- Проверить конфигурацию health check в docker-compose.yml
- Проверить nginx.conf на наличие ошибок
- Просмотреть детальные логи nginx: `docker logs codelab-ai-service-nginx-1`

---

### 2. **LiteLLM Proxy - Ошибка 400 при переключении агентов**
**Сервис:** `agent-runtime-1`, `gateway-1`  
**Код ошибки:** 400  
**Сообщение:**
```
No tool output found for function call call_pd6oIrxf502Zly3FqJfkJj9W
```

**Детали:**
```json
{
  "error": {
    "message": "litellm.BadRequestError: OpenrouterException",
    "code": 400,
    "metadata": {
      "raw": "No tool output found for function call call_pd6oIrxf502Zly3FqJfkJj9W",
      "provider_name": "Azure",
      "is_byok": false
    }
  },
  "user_id": "user_30eaodv99bipcmePxKOZTJzp2Cu"
}
```

**Контекст:**
- Ошибка возникает при попытке переключения с агента `ask` на агент `coder`
- Агент вызвал функцию `switch_mode` с аргументами: `{"mode": "coder", "reason": "User requested to run command: dart analyze"}`
- LLM провайдер (OpenRouter/Azure) не получил результат выполнения tool call

**Последовательность событий:**
1. Пользователь отправил: "вызови dart analyze"
2. Агент `ask` решил переключиться на агента `coder`
3. Создан tool call: `call_pd6oIrxf502Zly3FqJfkJj9W` для функции `switch_mode`
4. При следующем запросе к LLM провайдер не нашел результат этого tool call
5. Возвращена ошибка 400

**Корневая причина:**
Проблема в протоколе взаимодействия между agent-runtime и LLM провайдером при переключении агентов. После вызова tool `switch_mode`, система не отправляет результат выполнения этого tool обратно в LLM, что приводит к ошибке при следующем запросе.

---

### 3. **Ошибки 500 при запросе pending-approvals**
**Сервис:** `nginx-1` → `gateway` → `agent-runtime`  
**Код ошибки:** 500  
**Endpoint:** `/api/v1/sessions/{session_id}/pending-approvals`

**Примеры:**
```
GET /api/v1/sessions/1611205e-70ee-401b-8832-ce46e7b489be/pending-approvals HTTP/1.1" 500 36
GET /api/v1/sessions/1d5f458f-bf17-43ce-9475-4cfa14533acd/pending-approvals HTTP/1.1" 500 36
GET /api/v1/sessions/62df7e21-e0c8-4e90-ab89-28378c0dadb5/pending-approvals HTTP/1.1" 500 36
GET /api/v1/sessions/a65ee243-befa-4953-a56a-52864582c060/pending-approvals HTTP/1.1" 500 36
```

**Проблема:**
- Endpoint возвращает Internal Server Error (500)
- Размер ответа всего 36 байт (вероятно, короткое сообщение об ошибке)
- Ошибка повторяется для всех сессий

**Рекомендации:**
- Проверить реализацию endpoint в agent-runtime
- Добавить логирование для выявления причины ошибки
- Проверить доступ к базе данных для этого endpoint

---

## ⚠️ Предупреждения

### 4. **Redis - Отсутствие конфигурационного файла**
**Сервис:** `redis-1`  
**Сообщение:**
```
Warning: no config file specified, using the default config.
In order to specify a config file use redis-server /path/to/redis.conf
```

**Влияние:** Низкое  
**Рекомендация:** Создать redis.conf для production окружения с оптимальными настройками

---

### 5. **Множественные перезапуски Redis**
**Наблюдение:** Redis перезапускался несколько раз:
- 18:01:00 → 18:01:08
- 18:01:37 → 18:01:43
- 18:02:19 → 18:02:39
- 18:14:54 → 18:15:03
- 20:14:52 → 20:18:51
- 20:22:49 → 20:23:19

**Причина:** Получение SIGTERM (graceful shutdown)  
**Влияние:** Среднее - может привести к потере данных в памяти между перезапусками

---

## ✅ Работающие компоненты

### Успешные сервисы:
- ✅ **agent-runtime** - Healthy, обрабатывает запросы
- ✅ **auth-service** - Healthy, выдает токены
- ✅ **gateway** - Healthy, маршрутизирует запросы
- ✅ **llm-proxy** - Healthy, проксирует запросы к LLM
- ✅ **postgres** - Healthy, база данных работает
- ✅ **redis** - Healthy (несмотря на перезапуски)

### Успешные операции:
- Создание сессий (POST /api/v1/sessions)
- Получение списка сессий (GET /api/v1/sessions)
- WebSocket соединения (GET /api/v1/ws/{session_id})
- OAuth токены (POST /oauth/token)
- Health checks для всех сервисов

---

## 🔧 Рекомендации по исправлению

### Приоритет 1 (Критично):
1. **Исправить ошибку переключения агентов:**
   - Добавить отправку результата tool call `switch_mode` обратно в LLM
   - Или изменить логику переключения агентов, чтобы не требовать tool call response
   - Проверить файл: `codelab-ai-service/agent-runtime/app/domain/agent_switching.py`

2. **Исправить endpoint pending-approvals:**
   - Добавить обработку ошибок и логирование
   - Проверить SQL запросы к базе данных
   - Проверить файл: `codelab-ai-service/agent-runtime/app/api/sessions.py`

3. **Исправить Nginx health check:**
   - Проверить конфигурацию в `codelab-ai-service/nginx/nginx.conf`
   - Проверить healthcheck в `codelab-ai-service/docker-compose.yml`

### Приоритет 2 (Важно):
4. Добавить конфигурацию Redis для production
5. Исследовать причину частых перезапусков Redis
6. Добавить мониторинг и алертинг для ошибок 500

---

## 📊 Статистика

- **Всего сервисов:** 7
- **Healthy:** 6
- **Unhealthy:** 1 (nginx)
- **Критических ошибок:** 3
- **Предупреждений:** 2
- **Успешных операций:** Множество (создание сессий, WebSocket, OAuth)

---

## 🔍 Команды для дальнейшей диагностики

```bash
# Детальные логи nginx
docker logs codelab-ai-service-nginx-1 --tail=200

# Логи agent-runtime с фильтром по ошибкам
docker logs codelab-ai-service-agent-runtime-1 --tail=500 | grep -i error

# Логи gateway с фильтром по 500 ошибкам
docker logs codelab-ai-service-gateway-1 --tail=500 | grep "500"

# Проверка health check nginx
docker inspect codelab-ai-service-nginx-1 | grep -A 20 Health

# Подключение к Redis для проверки данных
docker exec -it codelab-ai-service-redis-1 redis-cli

# Подключение к Postgres для проверки таблиц
docker exec -it codelab-ai-service-postgres-1 psql -U postgres
```
