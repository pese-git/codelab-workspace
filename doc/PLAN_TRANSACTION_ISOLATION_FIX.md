# Исправление проблемы изоляции транзакций при создании плана

## 🐛 Проблема

При выполнении планов обнаружена критическая проблема: **план создается в одной транзакции, но не виден при попытке чтения в другой транзакции**.

### Симптомы

```
Plan {plan_id} not found for status update
```

План создается успешно, но при попытке прочитать его для обновления статуса на `APPROVED` возвращается `None`.

## 🔍 Корневая причина

### Проблема изоляции транзакций

SQLAlchemy использует изоляцию транзакций по умолчанию. Когда план создается и сохраняется через `flush()`, изменения видны только внутри текущей транзакции. Другие транзакции не видят эти изменения до выполнения `commit()`.

### Flow проблемы

1. **Создание плана** ([`architect_agent.py:243`](../codelab-ai-service/agent-runtime/app/agents/architect_agent.py:243)):
   ```python
   await self.plan_repository.save(plan)
   ```
   - Вызывается `PlanRepositoryImpl.save()` который делает `flush()` но НЕ `commit()`
   - План сохранен в БД, но транзакция не закоммичена
   - Commit происходит только когда завершается `get_db()` context manager

2. **Чтение плана** ([`plan_approval_handler.py:162`](../codelab-ai-service/agent-runtime/app/domain/services/plan_approval_handler.py:162)):
   ```python
   plan = await self._plan_repository.find_by_id(plan_id)
   ```
   - Это происходит в **ДРУГОЙ транзакции** (другой вызов `get_db()`)
   - План еще не закоммичен в первой транзакции
   - Из-за изоляции транзакций план не виден → возвращается `None`

### Архитектурная причина

**Dependency Injection через `get_db()`** создает новую транзакцию для каждого handler'а:

```python
async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with async_session_maker() as session:
        try:
            yield session
            await session.commit()  # Commit ПОСЛЕ yield
        except Exception as e:
            await session.rollback()
            raise
```

Проблема: commit происходит только после завершения всего handler'а, но план нужен раньше.

## ✅ Решение

### Подход: Явный commit для межтранзакционной видимости

Добавлен опциональный параметр `commit` в метод `save()` репозитория для случаев, когда требуется немедленная видимость данных в других транзакциях.

### Изменения

#### 1. PlanRepositoryImpl - добавлен параметр commit

**Файл**: [`plan_repository_impl.py:59`](../codelab-ai-service/agent-runtime/app/infrastructure/persistence/repositories/plan_repository_impl.py:59)

```python
async def save(self, entity: Plan, commit: bool = False) -> None:
    """
    Сохранить план.
    
    Args:
        entity: Доменная сущность плана
        commit: Если True, выполнить commit сразу (для межтранзакционной видимости)
    """
    try:
        # ... логика сохранения ...
        
        await self._db.flush()
        
        # Явный commit если требуется межтранзакционная видимость
        if commit:
            await self._db.commit()
            logger.debug(f"Saved and committed plan {entity.id}")
        else:
            logger.debug(f"Saved plan {entity.id} (commit deferred)")
```

**Обоснование**:
- По умолчанию `commit=False` - сохраняется существующее поведение
- `commit=True` - для критических случаев, когда план должен быть виден в других транзакциях

#### 2. ArchitectAgent - commit при создании плана

**Файл**: [`architect_agent.py:243`](../codelab-ai-service/agent-runtime/app/agents/architect_agent.py:243)

```python
# ВАЖНО: Используем commit=True для немедленной видимости в других транзакциях
# Это критично, т.к. план будет читаться в другой транзакции при approval
await self.plan_repository.save(plan, commit=True)

logger.info(
    f"Plan {plan.id} created and committed successfully with "
    f"{len(plan.subtasks)} subtasks"
)
```

**Обоснование**:
- План создается в одной транзакции (через Orchestrator)
- План читается в другой транзакции (через PlanApprovalHandler)
- Без commit план не будет виден при чтении

#### 3. PlanApprovalHandler - commit при обновлении статуса

**Файл**: [`plan_approval_handler.py:166`](../codelab-ai-service/agent-runtime/app/domain/services/plan_approval_handler.py:166)

```python
# Используем commit=True для немедленной видимости в других транзакциях
await self._plan_repository.save(plan, commit=True)
logger.info(f"Plan {plan_id} status updated to APPROVED and committed")
```

**Обоснование**:
- Статус плана обновляется в одной транзакции
- План будет читаться в другой транзакции при выполнении
- Commit гарантирует видимость обновленного статуса

## 📊 Результат

### До исправления

```
1. Orchestrator создает план → flush() → транзакция НЕ закоммичена
2. PlanApprovalHandler читает план → другая транзакция → план НЕ виден
3. Ошибка: "Plan {plan_id} not found for status update"
```

### После исправления

```
1. Orchestrator создает план → flush() → commit() → план виден всем
2. PlanApprovalHandler читает план → другая транзакция → план ВИДЕН ✅
3. Статус обновлен → commit() → изменения видны всем ✅
```

## 🎯 Когда использовать commit=True

### Используйте commit=True когда:

1. **Межтранзакционная видимость критична**
   - Данные будут читаться в другой транзакции
   - Данные нужны немедленно, не дожидаясь завершения handler'а

2. **Создание критичных сущностей**
   - Планы, которые будут использоваться в approval flow
   - Сущности, на которые будут ссылаться другие операции

3. **Обновление статусов**
   - Изменения статуса должны быть видны немедленно
   - Другие компоненты зависят от актуального статуса

### НЕ используйте commit=True когда:

1. **Обычные операции в рамках одной транзакции**
   - Все операции выполняются в одном handler'е
   - Commit произойдет автоматически в конце

2. **Промежуточные сохранения**
   - Сохранение прогресса выполнения
   - Обновления, которые не критичны для других транзакций

3. **Batch операции**
   - Множественные сохранения должны быть атомарными
   - Commit должен быть один в конце всех операций

## 🔧 Альтернативные решения (не выбраны)

### Вариант 2: Использовать одну транзакцию для всего flow

**Плюсы**:
- Атомарность всей операции
- Нет проблем с изоляцией

**Минусы**:
- Требует значительного рефакторинга архитектуры
- Нарушает принцип разделения ответственности
- Длинные транзакции могут вызвать блокировки

### Вариант 3: Изменить уровень изоляции транзакций

**Плюсы**:
- Решает проблему на уровне БД

**Минусы**:
- Может привести к race conditions
- Снижает консистентность данных
- Не рекомендуется для production

## 📝 Связанные документы

- [`TOOL_EXECUTION_FIX_COMPLETE.md`](TOOL_EXECUTION_FIX_COMPLETE.md) - Исправление tool execution flow
- [`TOOL_EXECUTION_FINAL_ROOT_CAUSE.md`](TOOL_EXECUTION_FINAL_ROOT_CAUSE.md) - Корневая причина проблемы с инструментами
- [`PLANNING_SYSTEM_FINAL_REPORT.md`](PLANNING_SYSTEM_FINAL_REPORT.md) - Архитектура системы планирования

## ⚠️ Важные замечания

1. **Не злоупотребляйте commit=True**
   - Используйте только когда действительно необходимо
   - Каждый commit - это дополнительная нагрузка на БД

2. **Тестирование**
   - Проверьте, что план виден после создания
   - Проверьте, что статус обновляется корректно
   - Проверьте, что нет race conditions

3. **Мониторинг**
   - Следите за логами: "created and committed" vs "commit deferred"
   - Отслеживайте количество commit'ов
   - Проверяйте производительность БД

## 🚀 Следующие шаги

1. ✅ Реализовано исправление
2. ⏳ Тестирование в реальных условиях
3. ⏳ Мониторинг производительности
4. ⏳ Документирование best practices для команды
