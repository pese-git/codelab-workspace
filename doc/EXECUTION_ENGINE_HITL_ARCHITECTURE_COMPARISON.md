# Сравнение архитектурных подходов для HITL синхронизации

## 🎯 Проблема

ExecutionEngine должен ждать HITL approval перед переходом к следующей subtask.

## 📊 Сравнение вариантов

### Вариант 1: Polling (Текущее предложение)

**Описание**: ExecutionEngine проверяет ApprovalManager каждые 0.5s

```python
while True:
    pending = await approval_manager.get_pending_by_session(session_id)
    if not pending:
        return  # Все разрешены
    await asyncio.sleep(0.5)
```

#### ✅ Плюсы
- **Простота**: Минимальный код, легко понять
- **Надежность**: Не зависит от событий/очередей
- **Отладка**: Легко логировать и отслеживать
- **Timeout**: Встроенная защита от зависания
- **Минимальные изменения**: Только ExecutionEngine + 1 метод в ApprovalManager

#### ❌ Минусы
- **Polling overhead**: Проверка каждые 0.5s (но это ~2 запроса/сек)
- **Задержка**: До 0.5s между approval и продолжением
- **CPU**: Постоянные проверки (но asyncio.sleep не блокирует)

#### 🏗️ Архитектурная оценка
- **Coupling**: Низкий (только зависимость от ApprovalManager)
- **Testability**: Отличная (легко мокировать)
- **Scalability**: Хорошая (для 1-10 concurrent executions)
- **Production-ready**: ✅ ДА (простой и надежный)

---

### Вариант 2: Event-based (Reactive)

**Описание**: ApprovalManager публикует события, ExecutionEngine подписывается

```python
# ExecutionEngine
event = asyncio.Event()
approval_manager.subscribe("approval_resolved", lambda: event.set())
await event.wait()  # Ждать события
```

#### ✅ Плюсы
- **Реактивность**: Мгновенная реакция на approval
- **Эффективность**: Нет polling overhead
- **Масштабируемость**: Хорошо для многих concurrent executions
- **Современный подход**: Event-driven architecture

#### ❌ Минусы
- **Сложность**: Требует event bus/pub-sub инфраструктуру
- **Debugging**: Сложнее отследить flow событий
- **Race conditions**: Риск пропустить событие
- **Timeout**: Нужна дополнительная логика
- **Больше кода**: Event bus, subscriptions, cleanup

#### 🏗️ Архитектурная оценка
- **Coupling**: Средний (зависимость от event bus)
- **Testability**: Средняя (нужно мокировать события)
- **Scalability**: Отличная (для 100+ concurrent executions)
- **Production-ready**: ⚠️ ТРЕБУЕТ ИНФРАСТРУКТУРЫ

---

### Вариант 3: Pause/Resume State Machine

**Описание**: ExecutionEngine имеет состояния RUNNING/PAUSED/RESUMED

```python
class ExecutionState(Enum):
    RUNNING = "running"
    PAUSED = "paused"
    RESUMED = "resumed"
    CANCELLED = "cancelled"

# ExecutionEngine
self.state = ExecutionState.PAUSED
await self._wait_for_state_change(ExecutionState.RESUMED)
```

#### ✅ Плюсы
- **Явное управление**: Четкие состояния
- **Отмена**: Можно отменить выполнение
- **Мониторинг**: Легко отслеживать состояние
- **Гибкость**: Можно добавить больше состояний

#### ❌ Минусы
- **Сложность**: State machine + transitions
- **Persistence**: Нужно сохранять состояние (для recovery)
- **Concurrency**: Сложнее с параллельными executions
- **Больше кода**: State management, transitions, validation

#### 🏗️ Архитектурная оценка
- **Coupling**: Высокий (state management во всех компонентах)
- **Testability**: Средняя (нужно тестировать все transitions)
- **Scalability**: Средняя (state management overhead)
- **Production-ready**: ⚠️ OVERKILL для текущей задачи

---

## 🎯 Рекомендация для Production

### **Вариант 1 (Polling)** - ЛУЧШИЙ выбор

#### Почему?

1. **KISS принцип**: Простое решение для простой задачи
2. **Надежность**: Меньше moving parts = меньше bugs
3. **Быстрая реализация**: 1-2 часа vs 1-2 дня
4. **Легкая поддержка**: Любой разработчик поймет код
5. **Достаточная производительность**: 
   - Polling каждые 0.5s = 2 запроса/сек
   - Для 10 concurrent executions = 20 запросов/сек
   - Это **ничтожная** нагрузка для современного сервера

#### Когда НЕ подходит?

- **100+ concurrent plan executions** → Тогда Event-based
- **Микросекундная задержка критична** → Тогда Event-based
- **Нужна отмена/пауза/возобновление** → Тогда State Machine

#### Но для текущего случая:

- ✅ Обычно 1-5 concurrent executions
- ✅ Задержка 0.5s приемлема (пользователь думает дольше)
- ✅ Простота важнее микрооптимизаций

---

## 🔄 Эволюция архитектуры

### Фаза 1: Polling (СЕЙЧАС)
- Быстро реализовать
- Протестировать в production
- Собрать метрики

### Фаза 2: Если нужно (ПОЗЖЕ)
- Если polling становится bottleneck → Event-based
- Если нужна отмена → State Machine
- Если нужна persistence → State Machine + DB

---

## 📈 Production метрики для мониторинга

После реализации Polling, отслеживать:

1. **Среднее время ожидания approval**: Должно быть < 30s
2. **Количество timeouts**: Должно быть < 1%
3. **Concurrent executions**: Обычно < 10
4. **CPU overhead от polling**: Должен быть < 0.1%

Если метрики показывают проблемы → Переходить к Event-based.

---

## 🎯 Финальная рекомендация

**Реализовать Вариант 1 (Polling)**

### Причины:
1. ✅ **Production-ready** из коробки
2. ✅ **Простой** и понятный
3. ✅ **Надежный** (меньше bugs)
4. ✅ **Быстрая реализация** (1-2 часа)
5. ✅ **Легкая поддержка**
6. ✅ **Достаточная производительность** для текущих нагрузок

### Когда пересмотреть:
- Если concurrent executions > 50
- Если polling overhead > 1% CPU
- Если нужна отмена/пауза

### Вывод:
**"Premature optimization is the root of all evil"** - Donald Knuth

Polling - это **правильный** выбор для production **сейчас**.
Event-based - это **оптимизация** для **будущего** (если понадобится).
