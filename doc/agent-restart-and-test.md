# Инструкция по перезапуску и тестированию исправлений

## Проблема

После внесения изменений в [`prompts.py`](../codelab-ai-service/agent-runtime/app/core/agent/prompts.py) и [`llm_stream_service.py`](../codelab-ai-service/agent-runtime/app/services/llm_stream_service.py), агент продолжает вызывать множественные `read_file` для несуществующих файлов.

**Причина:** Изменения в коде применяются только после перезапуска сервиса.

## Шаги для применения исправлений

### 1. Перезапуск agent-runtime сервиса

#### Вариант A: Docker Compose (если используется)

```bash
cd codelab-ai-service/agent-runtime
docker-compose restart
```

или полная пересборка:

```bash
cd codelab-ai-service/agent-runtime
docker-compose down
docker-compose up --build -d
```

#### Вариант B: Локальный запуск

Если сервис запущен локально, остановите его (Ctrl+C) и запустите заново:

```bash
cd codelab-ai-service/agent-runtime
# Остановить текущий процесс (Ctrl+C)
# Затем запустить снова
python -m uvicorn app.main:app --reload --port 8001
```

или с использованием uv:

```bash
cd codelab-ai-service/agent-runtime
uv run uvicorn app.main:app --reload --port 8001
```

### 2. Проверка загрузки промпта

После перезапуска проверьте логи, чтобы убедиться, что новый промпт загружен:

```bash
# Для Docker
docker logs codelab-agent-runtime | grep "CRITICAL"

# Для локального запуска - смотрите вывод в терминале
```

Вы должны увидеть, что сервис запустился без ошибок.

### 3. Создание новой сессии

**ВАЖНО:** Старые сессии используют старый промпт. Нужно создать **новую сессию** в IDE:

1. Закройте текущий чат с агентом
2. Откройте новый чат
3. Это создаст новую сессию с обновленным промптом

### 4. Тестирование

Попробуйте простой запрос, который раньше вызывал проблему:

```
Запрос: "Прочитай файл pubspec.yaml"
```

**Ожидаемое поведение:**
- Агент вызывает только ОДИН инструмент: `read_file("pubspec.yaml")`
- Ждет результата
- Если файл не найден, сообщает об ошибке
- НЕ пытается вызвать другие инструменты одновременно

**Неправильное поведение (старое):**
- Агент вызывает множество `read_file` одновременно
- Пытается читать несуществующие файлы
- Множественные ошибки "file_not_found"

### 5. Проверка логов agent-runtime

Проверьте логи на наличие предупреждений о множественных tool calls:

```bash
# Для Docker
docker logs codelab-agent-runtime | grep "VALIDATION"

# Для локального запуска
# Смотрите вывод в терминале, ищите строки с [Agent][VALIDATION]
```

**Если видите:**
```
[Agent][VALIDATION] LLM attempted to call 3 tools simultaneously!
This violates the one-tool-at-a-time rule.
Tools: ['read_file', 'read_file', 'read_file']
```

Это означает, что:
- ✅ Валидация работает
- ❌ LLM все еще пытается вызвать несколько инструментов
- 🔧 Возможно, нужно усилить промпт или использовать другую модель

**Если НЕ видите предупреждений:**
- ✅ LLM следует правилу "один инструмент за раз"
- ✅ Исправление работает корректно

### 6. Расширенное тестирование

Попробуйте более сложный сценарий:

```
Запрос: "Создай новый Flutter проект с именем my_test_app"
```

**Ожидаемая последовательность:**

1. Агент: `execute_command("flutter create -t app my_test_app")`
   - Ждет завершения команды
   
2. Агент: `list_files("my_test_app")`
   - Проверяет созданные файлы
   
3. Агент: `read_file("my_test_app/pubspec.yaml")`
   - Читает конфигурацию
   
4. Агент: `attempt_completion("Проект создан успешно")`

**Каждый шаг выполняется ПОСЛЕДОВАТЕЛЬНО**, с ожиданием результата.

## Диагностика проблем

### Проблема: Агент все еще вызывает множественные инструменты

**Возможные причины:**

1. **Старая сессия**
   - Решение: Создайте новую сессию (закройте и откройте чат)

2. **Сервис не перезапущен**
   - Решение: Перезапустите agent-runtime

3. **Кэширование промпта**
   - Решение: Полная пересборка Docker образа

4. **LLM модель игнорирует инструкции**
   - Решение: Попробуйте другую модель или усильте промпт

### Проблема: Логи не показывают предупреждения

**Проверьте:**

1. Уровень логирования:
   ```python
   # В app/core/config.py
   LOG_LEVEL = "DEBUG"  # Должен быть DEBUG или INFO
   ```

2. Формат логов:
   ```bash
   # Логи должны содержать [Agent][VALIDATION]
   grep -r "VALIDATION" codelab-ai-service/agent-runtime/app/
   ```

### Проблема: Файлы все еще не найдены

**Это нормально, если:**
- Файлы действительно не существуют
- Агент пытается прочитать файл до его создания

**Проверьте:**
1. Существует ли файл в файловой системе?
2. Правильный ли путь к workspace?
3. Выполнилась ли команда `flutter create` успешно?

## Мониторинг в реальном времени

Для отслеживания работы агента в реальном времени:

```bash
# Docker
docker logs -f codelab-agent-runtime

# Локальный запуск
# Логи выводятся в терминал автоматически
```

Ищите строки:
- `[Agent] Tool call detected: tool_name=...` - вызов инструмента
- `[Agent][VALIDATION]` - предупреждения о нарушениях
- `[Agent] Tool '...' result:` - результаты выполнения

## Успешное тестирование

Исправление работает корректно, если:

✅ Агент вызывает только ОДИН инструмент за раз  
✅ Ждет результата перед следующим вызовом  
✅ Не пытается читать несуществующие файлы до их создания  
✅ Логи не содержат предупреждений `[Agent][VALIDATION]`  
✅ Последовательность: tool → result → analyze → next tool  

## Дополнительные ресурсы

- [Описание проблемы и решения](./agent-tool-usage-fix.md)
- [Best practices для агента](./agent-best-practices.md)
- [Системный промпт](../codelab-ai-service/agent-runtime/app/core/agent/prompts.py)
- [Валидация tool calls](../codelab-ai-service/agent-runtime/app/services/llm_stream_service.py)

---

**Важно:** После каждого изменения в коде agent-runtime требуется перезапуск сервиса и создание новой сессии для применения изменений.
