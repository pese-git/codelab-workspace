# Анализ: Требуется ли доработка benchmark-standalone под новый алгоритм планирования

## Дата анализа
2026-01-16

## Краткий ответ
**✅ НЕТ, доработка НЕ требуется** - benchmark-standalone уже полностью поддерживает систему планирования, но **⚠️ ТРЕБУЕТСЯ добавить поддержку механизма approve/reject планов**.

## Текущее состояние

### ✅ Что уже реализовано в benchmark-standalone

1. **Модели данных для планирования** ([`src/models.py`](src/models.py))
   - ✅ `ExecutionPlan` - таблица для хранения планов
   - ✅ `SubtaskExecution` - таблица для подзадач
   - ✅ Все необходимые поля и индексы

2. **Сбор метрик планирования** ([`src/collector.py`](src/collector.py))
   - ✅ `record_plan_created()` - запись создания плана
   - ✅ `record_subtask_started()` - начало подзадачи
   - ✅ `record_subtask_completed()` - завершение подзадачи
   - ✅ `record_plan_completed()` - завершение плана
   - ✅ `get_plan_metrics()` - получение метрик

3. **Обработка сообщений о планировании** ([`src/client.py`](src/client.py))
   - ✅ `plan_notification` - создание плана (строки 263-301)
   - ✅ `subtask_started` - начало подзадачи (строки 303-330)
   - ✅ `subtask_completed` - завершение подзадачи (строки 332-379)
   - ✅ `plan_completed` - завершение плана (строки 381-409)
   - ✅ Детальное логирование всех событий
   - ✅ Запись метрик в БД

4. **Отчеты с метриками планирования** ([`src/reporter.py`](src/reporter.py))
   - ✅ Раздел "Planning Metrics" в отчетах
   - ✅ Сравнение планирования между режимами
   - ✅ Статистика по подзадачам

5. **Тесты** ([`tests/test_planning_integration.py`](tests/test_planning_integration.py))
   - ✅ Комплексные тесты всех компонентов

### ❌ Что НЕ реализовано (новый функционал)

#### Механизм approve/reject планов

**Проблема**: В agent-runtime добавлен механизм подтверждения планов:
- План создается с `requires_approval=True`, `is_approved=False`
- Architect отправляет `plan_notification` с `is_final=True`
- Ожидается ответ `plan_decision` от клиента
- План выполняется только после `approve`

**Текущее поведение benchmark-standalone**:
```python
# src/client.py, строка 263
elif msg_type == "plan_notification":
    # Обрабатывает уведомление, но НЕ отправляет plan_decision
    plan_created = True
    # ... логирование и запись метрик ...
    # ❌ НЕТ отправки plan_decision
```

**Ожидаемое поведение**:
```python
elif msg_type == "plan_notification":
    # Обработать уведомление
    plan_created = True
    # ... логирование и запись метрик ...
    
    # ✅ Автоматически подтвердить план
    requires_approval = metadata.get("requires_approval", False)
    if requires_approval:
        await websocket.send(json.dumps({
            "type": "plan_decision",
            "plan_id": plan_id,
            "decision": "approve"
        }))
        logger.info(f"✅ Auto-approved plan: {plan_id}")
```

## Изменения в agent-runtime

### Новые компоненты (с 2026-01-15)

1. **Модели** ([`app/models/plan_models.py`](../codelab-ai-service/agent-runtime/app/models/plan_models.py))
   - `PlanDecision` - enum (approve/edit/reject)
   - `PlanUserDecision` - модель решения пользователя
   - `PlanAuditLog` - логирование решений

2. **Сервисы** ([`app/services/plan_manager.py`](../codelab-ai-service/agent-runtime/app/services/plan_manager.py))
   - Управление audit logs для планов
   - Аналог HITL Manager

3. **Схемы** ([`app/models/schemas.py`](../codelab-ai-service/agent-runtime/app/models/schemas.py))
   - `ExecutionPlan.requires_approval: bool = True`
   - `ExecutionPlan.is_approved: bool = False`

4. **Architect Agent** ([`app/agents/architect_agent.py`](../codelab-ai-service/agent-runtime/app/agents/architect_agent.py))
   - Устанавливает `requires_approval=True`
   - Отправляет `plan_notification` с `is_final=True`
   - Ожидает `plan_decision`

5. **API** ([`app/api/v1/endpoints.py`](../codelab-ai-service/agent-runtime/app/api/v1/endpoints.py))
   - Обработка `plan_decision` (approve/edit/reject)

6. **Gateway** ([`gateway/app/models/websocket.py`](../codelab-ai-service/gateway/app/models/websocket.py))
   - Модель `WSPlanDecision`
   - Пересылка `plan_decision` в agent-runtime

## Требуемые изменения

### 1. Обновить [`src/client.py`](src/client.py)

**Местоположение**: Строки 263-301 (обработка `plan_notification`)

**Изменение**:
```python
elif msg_type == "plan_notification":
    # Существующий код обработки...
    plan_created = True
    metadata = msg.get("metadata", {})
    plan_id = metadata.get("plan_id", "unknown")
    # ... логирование ...
    
    # Запись метрик
    await collector.record_plan_created(...)
    
    # ✅ НОВОЕ: Автоматическое подтверждение плана
    requires_approval = metadata.get("requires_approval", False)
    is_final = msg.get("is_final", False)
    
    if requires_approval and is_final:
        # Benchmark автоматически подтверждает все планы
        await websocket.send(json.dumps({
            "type": "plan_decision",
            "plan_id": plan_id,
            "decision": "approve"
        }))
        logger.info(f"✅ Auto-approved plan: {plan_id}")
        logger.info("   (Benchmark mode: all plans are auto-approved)")
```

**Обоснование**:
- Benchmark должен работать автоматически без участия человека
- Цель - тестирование выполнения планов, а не UI подтверждения
- Можно добавить опцию в `config.yaml` для изменения поведения

### 2. Добавить конфигурацию (опционально)

**Файл**: [`config.yaml`](config.yaml)

**Добавить**:
```yaml
benchmark:
  tasks_file: "tasks.yaml"
  test_project: "./test_project"
  enable_validation: true
  
  # Новая опция
  plan_approval:
    auto_approve: true  # Автоматически подтверждать все планы
    # Будущие опции:
    # auto_reject: false
    # random_decision: false  # Для тестирования reject
```

### 3. Обновить документацию

**Файлы для обновления**:
- [`README.md`](README.md) - добавить информацию о plan approval
- [`ARCHITECTURE.md`](ARCHITECTURE.md) - обновить протокол
- [`PLANNING_INTEGRATION_GUIDE.md`](PLANNING_INTEGRATION_GUIDE.md) - добавить раздел об approve

## Рекомендации по реализации

### Приоритет: ВЫСОКИЙ ⚠️

**Почему критично**:
1. Без отправки `plan_decision` планы НЕ будут выполняться
2. Architect будет ждать подтверждения бесконечно
3. Все задачи с планированием будут проваливаться по timeout

### Этапы реализации

#### Этап 1: Минимальная реализация (30 минут)
```python
# В src/client.py, после записи метрик плана
if msg.get("is_final") and metadata.get("requires_approval"):
    await websocket.send(json.dumps({
        "type": "plan_decision",
        "plan_id": plan_id,
        "decision": "approve"
    }))
    logger.info(f"✅ Auto-approved plan: {plan_id}")
```

#### Этап 2: Конфигурируемое поведение (1 час)
- Добавить опции в `config.yaml`
- Поддержка `auto_approve`, `auto_reject`
- Логирование решений

#### Этап 3: Расширенное тестирование (2 часа)
- Тестирование approve
- Тестирование reject (для проверки обработки ошибок)
- Метрики по решениям

### Пример полной реализации

```python
# src/client.py

class GatewayClient:
    def __init__(self, ..., plan_auto_approve: bool = True):
        self.plan_auto_approve = plan_auto_approve
    
    async def execute_task(self, ...):
        # ... существующий код ...
        
        elif msg_type == "plan_notification":
            plan_created = True
            metadata = msg.get("metadata", {})
            plan_id = metadata.get("plan_id", "unknown")
            subtask_count = metadata.get("subtask_count", 0)
            subtasks = metadata.get("subtasks", [])
            requires_approval = metadata.get("requires_approval", False)
            is_final = msg.get("is_final", False)
            
            # Логирование плана
            logger.info("=" * 80)
            logger.info(f"📋 EXECUTION PLAN CREATED")
            logger.info(f"   Plan ID: {plan_id}")
            logger.info(f"   Total Subtasks: {subtask_count}")
            logger.info(f"   Requires Approval: {requires_approval}")
            logger.info("=" * 80)
            
            # Детали подзадач
            for idx, subtask in enumerate(subtasks, 1):
                # ... логирование подзадач ...
            
            logger.info("=" * 80)
            
            # Запись метрик
            await collector.record_plan_created(
                task_execution_id=task_execution_id,
                plan_id=plan_id,
                original_task=task_description,
                subtasks=subtasks
            )
            
            # Автоматическое подтверждение
            if requires_approval and is_final:
                decision = "approve" if self.plan_auto_approve else "reject"
                
                await websocket.send(json.dumps({
                    "type": "plan_decision",
                    "plan_id": plan_id,
                    "decision": decision
                }))
                
                decision_icon = "✅" if decision == "approve" else "❌"
                logger.info(f"{decision_icon} Auto-{decision}d plan: {plan_id}")
                logger.info(f"   (Benchmark mode: plan_auto_approve={self.plan_auto_approve})")
                logger.info("")
```

## Тестирование

### Тест 1: Проверка approve
```bash
# Запустить задачу, требующую планирования
python main.py --task-id task_009 --mode multi-agent

# Ожидаемый результат:
# 1. План создан
# 2. Отправлен plan_decision: approve
# 3. План выполнен
# 4. Все подзадачи завершены
```

### Тест 2: Проверка reject (опционально)
```bash
# В config.yaml установить plan_auto_approve: false
python main.py --task-id task_009 --mode multi-agent

# Ожидаемый результат:
# 1. План создан
# 2. Отправлен plan_decision: reject
# 3. План отменен
# 4. Получено сообщение об отмене
```

## Совместимость

### Обратная совместимость
✅ **Сохранена** - изменения не ломают существующий функционал:
- Старые планы без `requires_approval` работают как раньше
- Новые планы с `requires_approval` получают автоматическое подтверждение
- Все существующие метрики и отчеты работают

### Прямая совместимость
✅ **Обеспечена** - работает с новым agent-runtime:
- Поддержка `plan_decision` сообщений
- Обработка `requires_approval` флага
- Корректная работа с `is_final=True`

## Альтернативные подходы

### Вариант 1: Автоматическое подтверждение (рекомендуется)
**Плюсы**:
- ✅ Простая реализация
- ✅ Полностью автоматический режим
- ✅ Подходит для benchmark

**Минусы**:
- ❌ Не тестирует reject сценарий

### Вариант 2: Конфигурируемое поведение
**Плюсы**:
- ✅ Гибкость
- ✅ Можно тестировать разные сценарии
- ✅ Можно добавить случайные решения

**Минусы**:
- ❌ Более сложная реализация
- ❌ Требует обновления конфигурации

### Вариант 3: Игнорирование approval (НЕ рекомендуется)
**Плюсы**:
- ✅ Нет изменений в коде

**Минусы**:
- ❌ Планы не будут выполняться
- ❌ Все задачи будут проваливаться
- ❌ Benchmark станет бесполезным

## Заключение

### Итоговая оценка
- **Текущая поддержка планирования**: ✅ 95% (отлично)
- **Поддержка approve/reject**: ❌ 0% (отсутствует)
- **Критичность**: ⚠️ ВЫСОКАЯ
- **Сложность реализации**: 🟢 НИЗКАЯ (30 минут)

### Рекомендуемые действия

1. **Немедленно** (30 минут):
   - Добавить автоматическое подтверждение планов в `src/client.py`
   - Протестировать на задаче с планированием

2. **В ближайшее время** (1-2 часа):
   - Добавить конфигурацию `plan_auto_approve`
   - Обновить документацию
   - Добавить тесты

3. **Опционально** (будущее):
   - Поддержка `edit` решений
   - Случайные решения для стресс-тестирования
   - Метрики по решениям

### Влияние на метрики

**Без изменений**:
- ❌ Success Rate: ~0% (планы не выполняются)
- ❌ TTUA: timeout
- ❌ Все метрики планирования: 0

**С изменениями**:
- ✅ Success Rate: восстановлен
- ✅ TTUA: корректный
- ✅ Метрики планирования: полные

## Связанные документы

- [`PLANNING_INTEGRATION_GUIDE.md`](PLANNING_INTEGRATION_GUIDE.md) - Руководство по планированию
- [`PLAN_APPROVAL_IMPLEMENTATION_SUMMARY.md`](../PLAN_APPROVAL_IMPLEMENTATION_SUMMARY.md) - Реализация approve/reject
- [`ARCHITECTURE.md`](ARCHITECTURE.md) - Архитектура benchmark-standalone

---

**Автор**: AI Assistant  
**Дата**: 2026-01-16  
**Версия**: 1.0
